# This is my custom aliases file to be used with bash-it
# Create a symlink as:
# ln -s ~/git/dotfiles/custom.aliases.bash ~/.bash_it/aliases/custom.aliases.bash

cite 'about-alias'
about-alias 'my custom abbreviations'

alias conkeror='firefox --app ~/git/conkeror/application.ini'
alias ssh2q='ssh -F ~/prj/remote-dba/ssh_config'
alias ls='ls --group-directories-first --color=always'
